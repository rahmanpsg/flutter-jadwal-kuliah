import 'package:flutter/material.dart';

class CardJadwalLamaWidget extends StatelessWidget {
  const CardJadwalLamaWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return const Column(
      children: [],
    );
  }
}
